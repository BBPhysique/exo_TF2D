name: tf2d
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - matplotlib>=3.5.0
  - ipympl>=0.9.0
  - jupyterlab>=3.0.0
  - numpy
  - scipy
  - ipywidgets
  - nodejs  # needed for JupyterLab extensions